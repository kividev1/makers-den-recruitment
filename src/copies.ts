const copies = {
  errors: {
    'github.fetchError': 'Could not fetch suggestions',
    'general.networkError': 'No internet connection. Unable to fetch.'
  },
  search: {
    'searchbox.label': 'Type search phrase',
    'searchbox.noResults': 'No results'
  }
};

export default copies;
